#include <bits/stdc++.h>

using namespace std;

int main() {
    long long a, b;
    cin >> a >> b;
    cout << "a + b = " << a + b << "\n";
    cout << "a - b = " << a - b << "\n";
    if (b != 0) {
        cout << "a / b = " << 1.0 * a / b << "\n";
    }
}
